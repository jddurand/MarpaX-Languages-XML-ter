use Moops;

# PODCLASSNAME

# ABSTRACT: State role

role MarpaX::Languages::XML::Role::State {

  # VERSION

  # AUTHORITY

  requires 'origin';
  requires 'context';
  requires 'lastLexemes';
  requires 'namespaceSupport';
}

1;
