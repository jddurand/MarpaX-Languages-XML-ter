use Moops;

# PODCLASSNAME

# ABSTRACT: Plugin role

role MarpaX::Languages::XML::Role::Plugin {

  # VERSION

  # AUTHORITY

  requires 'grammar';
  requires 'subscriptions';
  requires 'plugin_register';
}

1;
